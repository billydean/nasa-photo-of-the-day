import React from "react";
import styled from 'styled-components';

function Nav () {
    return (
        <nav>
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Contact</a>
        </nav>
    )
}
export default Nav;